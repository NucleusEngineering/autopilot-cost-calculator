{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Autopilot Cost Calculator",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}"
        }
    ]
}